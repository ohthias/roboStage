export default function FllScoreLayout({ children }: { children: React.ReactNode }) {
  return (
    <section className="min-h-screen bg-base-100" aria-labelledby="fll-score-title">
      {children}
    </section>
  );
}
